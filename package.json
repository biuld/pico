{
  "name": "pico",
  "version": "0.1.0",
  "type": "module",
  "description": "Better Codex TUI — a richer terminal client for the official Codex app-server",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun run scripts/build.ts",
    "compile": "bun run scripts/build.ts --compile",
    "mock": "bun run tools/codex-app-server/start-manual-mock-pico.ts",
    "test": "bun test",
    "typecheck": "bun run scripts/typecheck.ts",
    "check": "bun run typecheck && bun test",
    "sync:codex-protocol": "bun run scripts/sync-codex-app-server-protocol.ts"
  },
  "dependencies": {
    "@opentui/core": "^0.2.6",
    "@opentui/solid": "^0.2.6",
    "@pico/codex-app-server-protocol": "workspace:*",
    "solid-js": "^1.9.12"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
